
export const metadata = {
  title: "Kredit Lab Dashboard",
  description: "Internal consultant system",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}