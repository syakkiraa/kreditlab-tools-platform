{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile",
    "watchPatterns": ["dashboard/**", "Dockerfile", ".dockerignore", "railway.json"]
  }
}
